import { post } from './post';
import { page } from './page';
import { subscriber } from './subscriber';
import { service } from './service';

export const schemaTypes = [post, page, subscriber, service];
